services:
  web:
    type: web
    name: my-django-app
    env: python
    buildCommand: |
      pip install -r requirements.txt
      pip install gunicorn
      python manage.py collectstatic --noinput
    startCommand: gunicorn electronic_bazaar.wsgi:application
    port: 8000